package io.github.texport.superkassa.jvm.settings

/**
 * Исключение, выбрасываемое при обнаружении некорректной конфигурации.
 */
class IllegalServerConfigurationException(message: String) : RuntimeException(message)
