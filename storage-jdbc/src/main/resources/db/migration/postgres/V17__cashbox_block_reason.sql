-- Причина блокировки кассы, полученная от ОФД.
-- Без неё касса, заблокированная ОФД, не разблокировалась сама никогда:
-- условие снятия блокировки сверяется именно с этим значением.
ALTER TABLE cashbox ADD COLUMN block_reason_code INTEGER;
