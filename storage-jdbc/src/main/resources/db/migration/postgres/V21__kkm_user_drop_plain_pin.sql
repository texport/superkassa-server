-- Пин кассира лежал рядом со своим хешем открытым текстом и уходил наружу
-- в списке пользователей. Вход сверяется по pin_hash, поэтому колонка
-- ничему не служила, а раздавала чужие учётные данные.
ALTER TABLE kkm_user DROP COLUMN IF EXISTS pin;
