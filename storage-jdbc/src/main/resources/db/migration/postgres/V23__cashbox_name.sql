-- Название кассы, данное владельцем.
-- Узел названия не хранил вовсе: на входе кассир видел регистрационный
-- номер, а данное владельцем название оставалось в кабинете и в настройках
-- одного рабочего места. Колонка пустая — у касс, заведённых раньше,
-- названия нет.
ALTER TABLE cashbox ADD COLUMN IF NOT EXISTS name TEXT;
